# 笔中枢延伸与扩展统一升级 v29 验收报告

## 版本与隔离边界

- 代码基线：`aab50c15f36385b4321819d79fa42c39617e923b`。
- 基线计算器指纹：`c37effc73acadfcab4589d1679f017d4c5afbc79b6b35d2397675e85d78bc10f`。
- v29 定义版本：`chan-period-unified-center-promotion-v29`。
- v29 层级版本：`center-hierarchy-v29-unified-promotion`。
- v29 最终计算器指纹：`4ea58462c77869e7382795ec5b230a6c6065b7659f79d3b662508f3eea88133a`。
- 活动 `8765` 服务保持 v28，10 个活动运行仍为指纹 `e294e06ae161f06660d0def68c8c4175d0dd035767b2b0577dcceefd4f6c46e5`。
- v29 仅在 `data/v29-candidate-20260920/v29-staging-final.db` 中重算并激活；未修改活动主库指针。

## 自动化验证

- 后端：`280 passed`，保留 6 个既有 FastAPI/Starlette 弃用警告。
- 前端：`179 passed`。
- 生产构建：通过；保留既有 `945.19 kB` 大包警告。
- `git diff --check`：通过。
- 活动主库：`PRAGMA quick_check=ok`，`PRAGMA foreign_key_check` 无结果。
- v29 staging：`PRAGMA quick_check=ok`，`PRAGMA foreign_key_check` 无结果。
- v29 结构审计：`10/10`。
- v29 API 审计：`10/10`。
- 父级来源元数据按候选触发时间只追加：后续入口不会回填较早父中枢修订；定向回归包含于后端全套。

## 隔离矩阵

表中数据依次为“活动中枢 / 活动走势 / 点位 / 活动升级候选 / 最高级别”；耗时为单次计算秒数。

| 标的周期 | aab50c1 基线 | v29 | 基线耗时 | v29 耗时 |
|---|---:|---:|---:|---:|
| `1A0001:5` | 5 / 2 / 5 / 0 / L1 | 5 / 2 / 5 / 3 / L1 | 0.012 | 0.020 |
| `1A0001:30` | 2 / 1 / 1 / 0 / L1 | 2 / 1 / 1 / 2 / L1 | 0.017 | 0.008 |
| `1A0001:d` | 18 / 3 / 33 / 0 / L1 | 18 / 8 / 33 / 21 / L1 | 0.062 | 0.139 |
| `1A0001:w` | 7 / 0 / 0 / 0 / L1 | 7 / 0 / 0 / 0 / L1 | 0.009 | 0.010 |
| `1A0001:m` | 1 / 0 / 0 / 0 / L1 | 1 / 0 / 0 / 0 / L1 | 0.003 | 0.002 |
| `1A0688:5` | 6 / 1 / 13 / 0 / L1 | 6 / 2 / 13 / 4 / L1 | 0.011 | 0.012 |
| `1A0688:30` | 5 / 1 / 6 / 0 / L1 | 5 / 2 / 6 / 4 / L1 | 0.005 | 0.006 |
| `1A0688:d` | 11 / 2 / 20 / 0 / L1 | 11 / 3 / 20 / 8 / L1 | 0.030 | 0.039 |
| `1A0688:w` | 3 / 0 / 0 / 0 / L1 | 3 / 0 / 0 / 0 / L1 | 0.005 | 0.005 |
| `1A0688:m` | 1 / 0 / 0 / 0 / L1 | 1 / 0 / 0 / 0 / L1 | 0.001 | 0.001 |

活动候选家族共 42 个：`expansion_decomposition/unresolved=28`，`extension_decomposition/unresolved=14`。当前活动运行的候选修订共 2097 条。真实矩阵没有满足独立三走势证明的动态或固定父中枢，因此最高级别仍为 L1；没有按九单位机械生成 L2。

## 上证日线 #12

- 目标中枢共 19 个拥有单位：3 核心、8 延伸、8 外围。
- 首个九单位前缀末端为 `R0-pen-104-2020-12-02-2020-12-11`。
- 该单位在 `2020-12-15` 可用，首个 `extension_decomposition` 候选的 `observed_at` 与 `evidence_available_at` 均为 `2020-12-15`。
- 候选保持 `unresolved`，缺失码为 `three_canonical_movements_missing`；未预设或伪造 L2。

## 浏览器验收

隔离服务地址为 `http://127.0.0.1:8766`，指向 final staging 数据库。

- 日线候选面板正确区分延伸候选、扩展未解、动态父中枢和固定父中枢，并显示稳定缺失码。
- 窄屏详情栏无文字覆盖；中枢矩形可点击，证据检查器显示精确家族、修订、核心单位和形成时间。
- 图表水平拖动触发带 `before` 的真实历史分页请求，并加载更早日期与结构。
- 周线、月线没有候选、走势或点位；日线 L2 当前为空，界面显示“当前范围暂无日线 L2”，未伪造投影。
- 动态/固定矩形和三段底层笔配色已由前端自动化测试覆盖；因真实矩阵没有动态/固定父中枢，不能声明已完成真实行情浏览器实测。

## 发布状态

未执行活动指针切换，未重启 `8765`，未覆盖活动运行。进入发布前仍需获得单独的活动切换确认；真实行情出现动态/固定父中枢后，还需补做对应矩形、三段高亮和日线 L2 周月投影的浏览器验收。
