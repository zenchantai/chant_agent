# v26 Z 段归属与证据驱动升级实施验收记录

## 1. 执行基线

- 正式计划：`../plans/2026-09-18-center-z-wave-expansion-fix.md`。执行开始已完整保存并立即回读核验。
- 版本：`chan-period-pen-recursive-non-same-level-v26`。
- 计算器指纹：`25fa9d8ab22cae365e0308ef649a00eba9dcc99b4442287ec510450195d5f935`。
- 保留工作区已有未提交修改；没有提交或推送代码。标准笔 `app/engine.py` 无本轮修改，原历史分页交互代码保留。
- 理论中的关系判定与本项目父核心公式分开记录；不将周期内 L2 解释为其他真实行情周期。

## 2. 已实现

1. 固定三单位核心与 Z 包络分离：Z 为第一、第三核心单位及被接纳的同向延伸；进入、连接和最终离开不扩大 DD/GG。上下文范围独立保存。
2. 按输入可用前缀推进状态机，记录形成、修订和升级可用时间；暂离返回、首次完整回试、边界接触分别处理。无法使用的进入前缀以稳定序列隔离，不伪造完成。
3. 互斥区分同级新生、扩张、接触候选；正式升级检查连续性、独立核心、真实连接组件、确认 Z 见证、源笔及事件时间。已被升级占用的并列配对标为失效，不能再显示为可升级候选。
4. 升级沿用较早家族并新增修订。历史修订保留原核心及形成证据；候选不关闭低级活动中枢。同区间形成路径合并，冲突保留先提交核心并输出问题。
5. 未完成走势按真实最高级别中枢重新分类；混合关系保持待定。确认低级走势不随升级改写，不将历史未完成修订伪装为完成子走势。趋势资格重新约束一类点，力度公式不变。
6. 持久化保存真实 `unit_kind` 和 `z_wave` 角色；计算、入库、独立审计复用数值及引用校验器。
7. API 保留分组响应并补齐分页引用闭包、全量层级信息；历史上下文不自动绘制。前端补齐无 L1 提示、显式 L2 切换、子修订展开、Z/上下文详情及分角色高亮。
8. 确认走势使用实线，provisional 使用虚线，undetermined/truncated 使用点线；历史子中枢详情明确标为历史证据修订。

## 3. 自动化检查

| 检查 | 实际结果 |
| --- | --- |
| `.venv/bin/python -m pytest -q` | 161 passed；6 个 FastAPI 生命周期弃用警告 |
| `npm test -- --run` | 138 passed / 8 files |
| `npm run build -- --outDir ../data/v26-release-20260918/final-dist` | 通过；有打包体积超过 500KB 的提示；成功后复制到 `web/dist` |
| `git diff --check` | 通过 |
| 重算 dry-run / execute | 两个启用标的 × 5/30/d/w/m，共 10 组成功 |
| 活动运行及真实 API 审计 | 10/10 成功，版本和指纹一致，无引用或外键错误 |
| HTTP 分页组合 | 2 标的 × 5 周期 × 0/1/2 层级 × 两种 diagnostics × 两页，共 120 次响应通过 |
| SQLite | `integrity_check=ok`，`foreign_key_check` 为空 |

测试包含计划三组价格序列及向下镜像、Z 源单位、进入极值排除、缺失连接/确认/序列时保留低级、扩张证据损坏拒绝、真实日线所有输入前缀的形成证据和确认走势稳定性、同区间合并及冲突保护。双路径合并测试为合成数据，不声称真实行情已经命中该路径。

## 4. 真实行情结果与逐对证据

当前全量日线活动结构（不是当前 300 根页面中可见数量）：

| 标的 | L1 | L2 |
| --- | ---: | ---: |
| 科创50 `1A0688` | 3 | 5 |
| 上证 `1A0001` | 5 | 8 |

这些数量来自规则结果，不作为测试目标。周线仍存在无活动 L1 的合法升级结果，前端必须明确提示而非自动回退。

示例可追溯升级：

- 科创50：`center-family-1feec30a94d552:r6`，父核心 `[719.12,827.63]`，首次证据时间 `2024-05-28`；两个子修订为 `center-family-1feec30a94d552:r5`、`center-family-6d2422ea56e970:r1`；连接 `component-5f1ebe0367b4b5`；重叠见证为 `R0-pen-70-2023-11-15-2024-02-05`、`R0-pen-72-2024-03-12-2024-04-22`。
- 上证：`center-family-16d82d4d8a704b:r13`，父核心 `[2822.185,2915.297]`，首次证据时间 `2019-07-03`；连接 `component-b0f4e2c70ed186`；重叠见证为 `R0-pen-62-2018-07-26-2018-08-20`、`R0-pen-77-2019-06-06-2019-07-02`。

完整逐对新旧核心、Z 包络、替代核心和原因保存在 `../../data/v26-release-20260918/v25-v26-comparison.json`：科创50 22 条、上证 34 条，包含已替代的旧核心说明，不等于活动中枢数量。

## 5. 数据切换与证据文件

- 两次切换均停止常驻服务，确认无数据库占用，使用一致性备份和 sidecar 重算；校验成功并 checkpoint/关闭连接后原子替换数据库，再恢复 `com.zenchant.chant-agent`。
- `verification.json` 和 `final-verification.json` 均为停服/候选库比较，八张保留表行数与 SHA256 全部一致。最后切换时 `market_bars=10222`，`drawing_objects=1`。
- 恢复服务后的旧 v25/实时 v26 比较文件 `v25-v26-comparison.json` 的总 `passed` 为 **false**：其八表严格相等条件发现恢复同步后增加的两条上证 `2026-09-18` 日线、周线。逐行检查旧行删除数为 0、新增数为 2；其余七表一致，结构矩阵 10/10 仍通过。不能将这一实时比较伪报为八表完全相等。
- 当前回滚数据库为 `../../data/v26-release-20260918/latest-source.db`，第一次 v26 切换后副本为 `pre-final.db`；没有清理这些副本。
- 运行审计：`live-audit.json`；分页检查：`live-pagination.json`；重算：`final-recalculate.json`；测试日志：`pytest.log`、`frontend-tests.log`、`frontend-build.log`，均位于上述 release 目录。

## 6. 浏览器实际检查

在真实 localhost 页面而非模拟 DOM 中执行：

- 日线 L1 矩形可见，点击显示固定核心、Z 包络、上下文、形成时间与真实笔引用；选中后进入/核心/离开/回试分色。
- 科创50周线无 L1 时显示“当前无活动 L1，可查看 L2”；点击按钮显示两个 L2，而不是静默切换。
- 选择 L2、展开子中枢并选择历史 L1 证据后，详情保持可读，图上活动中枢数量不增加；点击空白清除选择。
- 时间滑块拖动及滚轮触发历史加载，从最近的 `2025-07` 页面进入 `2024-04` 区间，并进一步加载到可查看 `2021-10-29` 的行情。
- 普通绘图可拖出测试线段，取消编辑后消失，未保存；保留用户原绘图。
- 最终前端已重载；浏览器捕获的 error 日志为空。后端健康接口显示 v26 和 `active_run_status=ready`。

## 7. 未完成验收、限制与计划差异

1. 没有在真实行情实时演变中观察一次“扩张候选 → 确认升级”；候选、接触和失效转换以自动化测试验证，不能等同于实时浏览器观察。
2. 直接在主图空白处拖动有未发生平移的操作；已验证时间滑块拖动和滚轮历史加载，但不能声明所有主图拖拽手势均通过。原分页/手势代码未在本轮另行重构。
3. 当前真实数据最高为 L2；L3—L8、纯高级扩张系统的后续组合及扩张/标准递归双路径重合没有真实行情验收样本，不伪造高级低级走势单位。
4. 自选栏当前标的存在暂时显示“等待行情”、主图仍有历史收盘数据的情况；未将其当作中枢修复，也未改动行情同步逻辑。
5. 约 18:15 发现原 `data/backups/v26-z-wave-20260918` 目录及其中报告/代码归档消失，清理来源未确认，已通知用户。当时正式库尚未替换，重新备份到不在该目录下的 `data/v26-release-20260918` 后才继续。v25 数据库和旧前端仍有回滚副本，但最初保存的精确未提交 v25 源码归档丢失，不能声称完整代码自动回滚已演练。
6. 因上述限制，不将本记录表述为“全部验收通过”；保留回滚副本，不执行计划末尾的副本清理。没有为了恢复 L1 数量而抑制具备完整证据的升级。
